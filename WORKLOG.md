# WORKLOG.md

작업 이력을 시간순으로 기록한다. 새 작업 세션을 시작할 때는 맨 아래에 이어서 추가한다.

## 2026-09-18

### 1. 요구사항 정의 (AGENTS.md / RULES.md)
- 목표: 공부한 날짜/과목/내용을 기록하면 목표 대비 달성률(%)과 교재 회독수를 볼 수 있는
  개인용 아이폰 앱.
- 결정 사항
  - 플랫폼: PWA (순수 HTML/CSS/JS, 빌드 도구 없음)
  - 데이터 저장: 로컬 저장(localStorage) + JSON 내보내기/가져오기 수동 백업
    (iOS Safari가 오래 미사용 시 로컬 저장소를 지울 수 있어 백업 기능을 필수로 둠)
  - 달성률 기준: 과목별 개별 목표 시간(주간/월간) 대비 달성률
  - 회독(교재 완독 횟수) 관리: 과목 아래 교재 단위, 사용자가 직접 "완료" 버튼을 눌러 카운트
  - 앱 이름: CTA, 테마: 파란 계열, 다크모드: 기기 설정 자동 반영
  - 배포: GitHub Pages 예정
- 산출물: `AGENTS.md`(목적/기능/데이터 모델/산출물), `RULES.md`(코딩 규칙)

### 2. v1 구현
- 파일 구성: `index.html`, `css/style.css`, `js/app.js`, `js/storage.js`, `js/stats.js`,
  `js/ui.js`, `manifest.json`, `service-worker.js`, `icons/icon-{180,192,512}.png`
- 화면 4개(탭 전환): 대시보드 / 기록 / 과목 / 설정
  - 대시보드: 이번 주·이번 달 달성률(미터), 누적 공부시간, 이번 주 과목별 공부시간
    막대그래프, 교재별 회독 현황
  - 기록: 오늘 기록 추가 폼(날짜·과목/교재·시간·내용), 최근 기록 목록(삭제 가능)
  - 과목: 과목 추가(주간/월간 목표시간), 과목별 교재 추가·삭제·회독 완료
  - 설정: JSON 내보내기/가져오기 백업, 마지막 백업 경과일 안내
- 데이터 모델: `Subject { weeklyGoalMinutes, monthlyGoalMinutes, materials[] }`,
  `Material { roundHistory[] }`, `StudyLog { date, subjectId, materialId?, durationMinutes, content }`
- 차트/미터 색상은 dataviz 스킬의 검증된 기본 팔레트(파란 계열 accent + 8색 카테고리 팔레트)를
  그대로 사용.

### 3. 동작 확인
- Node/Python이 실제로 설치돼 있지 않아(둘 다 Windows 스토어 자리표시자) PowerShell
  `System.Net.HttpListener`로 임시 정적 서버를 띄워 테스트.
- 헤드리스 Edge + Chrome DevTools Protocol(WebSocket)로 실제 사용 시나리오를 자동화해
  검증: 과목 추가 → 교재 추가 → 회독 완료 클릭 → 공부 기록 추가 → 대시보드 확인.
  - 달성률 계산 결과가 수식과 일치함을 확인 (예: 90분/600분 목표 = 15%).
  - 라이트 모드·다크 모드 스크린샷으로 레이아웃/가독성 확인.
- 테스트 중 자동화 스크립트(PowerShell 5.1)에서 한글 리터럴이 깨져 보이는 문제가 있었으나,
  이는 테스트 스크립트 자체의 인코딩 이슈였고 실제 앱 코드와는 무관함을 별도 확인
  (브라우저에 직접 시드 데이터를 넣어 재검증, 정상 표시됨). 실제 아이폰 키보드 입력과는
  무관한 이슈.

### 4. 사용자 피드백 반영
- 요청: "과목별 달성률도 보고 싶다", "총 누적 공부시간이 잘 안 보인다"
- 반영
  - 대시보드에 "과목별 달성률 · 누적 시간" 카드 추가: 과목마다 이번 주/이번 달 달성률(%)과
    해당 과목의 누적 공부시간을 함께 표시 (`stats.js`의 `subjectStats` 함수 추가).
  - 기존 "누적 공부시간" 카드 라벨을 "누적 공부시간 (전체 과목 합계)"로 명확화.
  - `service-worker.js` 캐시 버전 v1 → v2 (오래된 캐시로 인해 화면이 갱신되지 않는 문제 방지).

### 5. 버그 수정 + 세무사 2차 맞춤 기능
- 버그: 교재가 있는 과목도 기록 입력 시 "(교재 미지정)" 옵션이 항상 남아있던 문제 →
  교재가 하나도 없는 과목에서만 미지정 옵션이 뜨도록 수정 (`ui.js`의 `targetOptionsHTML`).
- 사용자가 세무사 2차 준비생이라는 맥락에 맞춰 아래 기능 추가
  - **D-day 카운터**: 설정에서 시험 이름·날짜 등록 → 대시보드 상단 배너로 "{이름} D-{일수}" 표시
  - **연속 학습일(스트릭)**: 오늘/어제까지 하루도 안 빠뜨렸는지 계산해 대시보드에 표시
    (`stats.js`의 `currentStreak`)
  - **기록 태그(오답노트/취약 유형)**: 기록에 쉼표 구분 태그를 남기고, 기록 탭에서 태그별 필터링

### 6. 일일 목표 · 시험 준비 캘린더 · 디자인 리프레시
- **하루 목표 시간** 설정 추가 (전체 과목 합산 기준, `data.meta.dailyGoalMinutes`)
- **시험 준비 캘린더**: 오늘부터 시험일까지를 ㅁ모양 셀로 표시, 그날 목표 달성 여부에 따라
  초록(달성)/노랑(일부)/빨강(미학습)/회색(예정)으로 색칠 (`stats.js`의 `examCalendarCells`,
  최대 120일까지 표시). dataviz 스킬의 고정 상태색(good/warning/critical)을 그대로 사용.
- **디자인 리프레시**: "토스 앱 느낌"으로 요청받아 카드 테두리 제거 후 그림자(box-shadow)로
  대체, 모서리를 더 둥글게(카드 20px), 큰 수치는 더 굵게, 버튼/탭 클릭 시 살짝 눌리는
  피드백(`:active { scale }`) 추가. 색상 팔레트 자체는 바꾸지 않고 형태·그림자·타이포만 조정.
- **폰트 교체**: 시스템 폰트 → Pretendard(오픈소스, 토스 등이 즐겨 쓰는 서체)로 변경,
  jsdelivr CDN에서 로드하되 서비스워커 프리캐시 목록에는 넣지 않아 오프라인 설치가
  막히지 않도록 함.

### 7. 회독 목표치 + 배너 UI 조정
- 교재마다 목표 회독수(선택)를 설정할 수 있게 하고, "2/5회독" 형태로 진행률 바와 함께 표시
  (`storage.js`의 `setMaterialTarget`).
- 대시보드 시험 배너에서 시험 이름 글자가 D-day 숫자보다 작아 보이던 것을 22px로 동일하게 맞춤.

### 8. 배포 (GitHub Pages)
- PC에 Git/GitHub CLI가 설치돼 있지 않아 `winget install --id Git.Git --scope user`로 설치.
- GitHub 계정 신규 가입(`chopu9377`), 저장소 `chopu9377/CTA` 생성.
- 이 실행 환경이 완전 비대화형(non-interactive)이라 `git push`의 GitHub 로그인 팝업을 띄울 수
  없었음 (사용자가 직접 `!` 명령으로 실행해도 동일하게 "terminal prompts disabled" 오류).
  → 토큰을 대화에 노출시키지 않기 위해 **GitHub 웹사이트에 파일을 직접 드래그 업로드**하는
  방식으로 우회 배포. 로컬 git 저장소/커밋은 남겨뒀으니 나중에 인증 방법(GitHub Desktop 등)을
  마련하면 그걸로 이어서 push 가능.
- Settings → Pages → Deploy from branch(main, `/root`)로 GitHub Pages 활성화.
- 배포 주소: **https://chopu9377.github.io/CTA/** — 아이폰 Safari에서 홈 화면 추가 확인 완료.

### 9. 데이터 안전성 보강
- 사용자 질문: "업데이트해서 배포하면 로컬 데이터 날아가는 거 아니냐" → 점검해보니
  실제 위험 요소 발견: `storage.js`가 저장된 데이터의 `schemaVersion`이 코드의 현재
  버전과 다르면 무조건 빈 데이터로 초기화하던 로직이 있었음 (지금까지는 버전을 한 번도
  올린 적이 없어 드러나지 않았을 뿐, 다음에 스키마를 바꾸는 순간 기존 사용자 데이터가
  통째로 날아갈 수 있는 잠재 버그였음).
- 수정: `normalize()` 함수를 도입해 `subjects`/`logs` 배열만 존재하면 누락된 필드를
  기본값으로 채워 넣는 방식으로 변경. 완전히 읽을 수 없는 경우에만 초기화. `importData`도
  동일 로직 사용. RULES.md 4절에 이 규칙을 명문화해 향후에도 지키도록 함.
- 결론: **코드를 업데이트/재배포해도 iOS Safari의 `localStorage`는 그대로 유지**되며
  (배포는 정적 파일을 바꾸는 것일 뿐 브라우저 저장소를 건드리지 않음), 위 수정으로
  스키마가 바뀌어도 데이터가 보존되도록 안전장치를 마련함. 유일한 남은 위험은 기존에
  알려진 "iOS Safari가 오래 미사용 시 저장소를 지울 수 있다"는 것뿐이며, 이는 JSON
  백업 기능으로 대응.

### 10. Git push 인증 해결
- PowerShell/도구 실행 환경이 완전 비대화형이라 GCM 로그인 팝업을 띄울 수 없었던 문제를,
  사용자가 **직접 여는 일반 PowerShell 창**에서 `git push` 실행 → 정상적으로 브라우저 로그인
  팝업이 떠서 해결. 이후로는 이 PC에서 자격 증명이 캐시되어 팝업 없이 바로 push 가능.
- 과정에서 로컬 git 기록과 (예전에 웹 업로드로 만들어진) 원격 기록이 서로 다른 뿌리를 가져
  `push`가 거부됨 → 로컬이 원격의 상위 호환(내용 전부 포함 + 최신 수정 포함)임을 확인하고
  `git push --force`로 원격을 로컬 기준으로 정리. 이후 `git push`만으로 배포되는 정상 흐름
  확립.

### 11. 주말 목표 분리 + 과목 목표 수정 기능
- 요청: "직장 병행이라 평일/주말 공부시간이 다른데, 주말 공부시간을 기존과 같은 방식으로
  따로 체크하고 싶다."
- 반영
  - 과목마다 `weekendGoalMinutes`(주말 목표) 필드를 추가. 기존 주간/월간 목표와 완전히
    독립적인 값으로, 안 정해도(0) 기존 동작에 영향 없음 (additive, 무마이그레이션).
  - `stats.js`에 `isWeekend(dateStr)` 추가, `periodSummary`/`subjectStats`가 주말(토·일)만
    따로 걸러 실적/목표/달성률을 계산하도록 확장.
  - 대시보드 상단에 "이번 주말 달성률" 타일 추가 (이번 주/이번 달과 동일한 형태),
    과목별 카드에도 "이번 주말" 미니미터 한 줄 추가.
  - 곁다리로 발견한 기존 공백 보완: 지금까지 과목의 주간/월간 목표는 **과목을 처음 만들
    때만** 정할 수 있고 나중에 고칠 방법이 없었음 → '과목' 탭에 과목별 "목표 수정" 폼을
    추가해 언제든 주간/월간/주말 목표를 다시 저장할 수 있게 함 (`storage.js`의
    `updateSubjectGoals`).
- 시드 데이터로 이번 주말 실적/목표/달성률 계산과 목표 수정 폼 저장까지 스크린샷으로 확인.

### 12. 개인 백업용 비공개 저장소 분리
- 질문: "백업 JSON을 깃에 저장해도 되나?" → 배포용 `CTA` 저장소는 GitHub Pages 무료 이용
  조건상 public이라, 개인 공부 기록까지 같이 올리면 전부 공개돼버리는 문제를 짚어드림.
- 해결: 별도의 **private** 저장소 `chopu9377/cat-backup` 을 새로 만들고, PC에
  `C:\Users\senki\OneDrive\바탕 화면\cta-backup` 폴더를 로컬 git 저장소로 연결.
  안에 `backup.bat`(더블클릭 한 번으로 add/commit/push 자동 처리), `백업방법.txt`
  (혹시 몰라 수동 3줄 명령어도 메모), `README.md`를 만들어둠.
- 아이폰에도 OneDrive가 연결되어 있어서, CTA 내보내기 시 OneDrive의
  `바탕 화면/cta-backup` 폴더에 바로 저장하면 파일을 따로 옮길 필요 없이 곧바로 이
  폴더에 나타나도록 안내함 (내보내기 → OneDrive 저장 → backup.bat 더블클릭, 2단계로 축약).
- 전역 git 사용자 설정(`git config --global`)도 이때 같이 잡아둬서, 앞으로 새 저장소를
  연결할 때마다 매번 이름/이메일을 다시 설정할 필요 없게 함.

### 13. 평일/주말 목표 분리 + 월간 목표 자동 계산
- 배경: 기존 "주말 목표" 추가 후, 사용자가 "주간 목표"를 사실상 "평일 목표"(1주간 평일
  합계)로 이해하고 있다는 게 드러남 — 즉 주간 목표와 주말 목표를 완전히 독립된
  두 축으로 보고, 월간 목표는 `(평일 목표 + 주말 목표) × 4주`로 자동 계산되길 원함.
- 반영
  - `stats.js`: `weeklyGoalMinutes`의 의미를 "평일(월~금) 목표"로 명확히 하고, 실적
    계산도 평일 로그만 걸러서 비교하도록 변경 (`periodSummary`/`subjectStats`의
    `week` → `weekday`로 리네이밍). `monthlyGoalFor(subject)` 헬퍼를 추가해 월간 목표를
    항상 `(평일+주말)×4`로 즉석 계산.
  - `storage.js`: `monthlyGoalMinutes` 필드를 더 이상 저장/입력받지 않음 (기존 저장된
    값이 있어도 그냥 무시됨, additive 원칙에 따라 별도 마이그레이션 없이 자연 폐기).
  - `ui.js`: 대시보드 "이번 주 달성률" → "이번 주 평일 달성률"로 명칭 변경(계산 기준도
    평일로 변경), "이번 달 달성률"은 자동 계산된 목표를 사용하며 "(자동)" 표시 추가.
    과목 추가/수정 폼에서 "월간 목표" 입력 필드를 없애고 "(평일+주말)×4주로 자동
    계산돼요" 안내 문구로 대체.
- 시드 데이터(평일 목표 3.5시간, 주말 목표 7시간)로 이번 주 평일 29%, 이번 주말 43%,
  이번 달 10%(실적 4시간/자동목표 42시간)까지 정확히 계산됨을 스크린샷으로 확인.

## 2026-09-19

### 14. 날짜별 목표 시간 배분('목표' 탭) + 공부량체크 탭 추가
- 배경: 사용자가 "그날그날 목표를 먼저 쓰고 기록하는" 흐름을 원함 — 예: 재무 3시간,
  원가 2시간, 세법 1시간, 세무회계 0시간처럼 그날 과목별로 시간을 배분해두면 하루
  목표가 자동으로 합산되고, 공부 기록을 쓰면서 그 할당량을 채웠는지 바로 확인하고
  싶어함. 추가로 주 평균/일 평균 공부시간을 보는 '공부량체크' 탭도 요청.
- 결정 사항 (사용자 확인)
  - 탭 구조: 새 탭 2개 추가(목표/공부량)로 대시보드·기록·과목·설정과 나란히 배치
    (총 6탭).
  - 기존 설정 탭의 "하루 목표 시간(전체 합산, 시험 캘린더용)" 입력은 완전히 없애고,
    그날 과목별 배분의 합으로 자동 대체.
  - 공부량체크 지표 범위: 전체 기간 평균 + 최근 7일/30일 평균 모두 표시.
- 반영
  - `storage.js`: `data.dailyPlans[date] = { [subjectId]: minutes }` 맵 추가
    (`setDailyPlan`/`getDailyPlan`). `deleteSubject`에서 과목 삭제 시 모든 날짜의
    `dailyPlans`에서도 해당 과목 키를 제거(기존 `logs` 정리와 같은 패턴). `meta.dailyGoalMinutes`는
    더 이상 쓰거나 읽지 않음(과거 `monthlyGoalMinutes` 폐기 때와 같은 additive 원칙 —
    기존 백업에 남아있어도 그냥 무시됨).
  - `stats.js`: `dailyPlanTotal`(배분 합계), `dailyPlanProgress`(날짜별 과목별/전체
    실적 대비 달성률), `studyVolumeStats`(전체 기간·최근 7일·최근 30일 일 평균/주 평균)
    추가. `examCalendarCells`는 이제 각 날짜 자체의 `dailyPlans` 합계를 그날의 목표로
    사용(고정된 전역 목표 대신).
  - `ui.js`/`app.js`: '목표' 탭(`renderGoal`) — 날짜 선택(기본 오늘) + 과목별 시간 입력
    폼 + 그 날짜 달성 현황. '공부량' 탭(`renderVolume`) — 전체/7일/30일 일 평균·주
    평균 타일. 설정 탭에서 하루 목표 입력 필드 제거, 안내 문구로 대체.
  - `index.html`/`css/style.css`: 탭바에 '목표'/'공부량' 버튼 추가, 6탭에 맞춰
    `.tab-btn` 폰트 크기·패딩 축소.
- 확인: Node/Python 미설치 상태라 이전과 동일하게 PowerShell `HttpListener`로 임시
  정적 서버를 띄우고, Claude in Chrome으로 실제 시나리오를 검증 — 과목 4개 추가 →
  '목표' 탭에서 재무3/원가2/세법1/세무회계0 배분(합계 6시간) → '기록' 탭에서 재무
  90분 기록 → '목표' 탭에서 전체 25%(1시간30분/6시간), 재무 50%(1시간30분/3시간)로
  정확히 갱신됨을 확인. '공부량' 탭에서 전체 기간 일평균 1시간30분(주평균
  10시간30분), 최근 7일 일평균 13분, 최근 30일 일평균 3분까지 수식과 일치. 대시보드
  시험 캘린더도 그날 배분(6시간) 대비 실적(25%)에 맞춰 "일부 학습"(노랑)으로 정확히
  표시됨을 확인.

### 15. 앱 아이콘 교체
- 배경: 사용자가 `icons/sample_icon.svg`(원형 순환/화살표 모양 로고, 색상 `#3d51a6`)를
  직접 추가해두고 이걸로 아이콘을 바꿔달라고 요청.
- 반영: Node/Python/ImageMagick 등 SVG 래스터라이저가 이 PC에 없어서(둘 다 Windows
  스토어 자리표시자만 있고 dotnet도 SDK 없이 런타임만 설치돼 있음), Chrome(클로드인크롬
  확장)으로 SVG를 실제 렌더링해 흰 배경 위 중앙에 배치한 화면을 스크린샷으로 저장하고,
  PowerShell `System.Drawing`으로 파란 원형 로고의 바운딩 박스를 픽셀 스캔해 정사각형으로
  자른 뒤, 흰 배경에 72% 크기로 센터 배치해 `icons/icon-{180,192,512}.png`를 재생성.
  (마스커블 아이콘 세이프존을 고려해 로고 주변에 여백을 둠.) `manifest.json`/`index.html`은
  기존 파일명을 그대로 참조하므로 변경 없음. `service-worker.js` 캐시 버전만
  `v11` → `v12`로 올려 기존 설치본에도 새 아이콘이 반영되게 함.
- 확인: 재생성된 PNG 3종을 직접 열어 로고가 선명하고 잘림 없이 중앙에 위치하는지 확인.

### 16. 대시보드 '과목별 달성률'을 원형 프로그레스로 교체
- 배경: 사용자가 기존 막대(퍼센트 텍스트 + 가로 바) 방식이 "더럽고 직관성이 떨어진다"고
  평가. 과목별로 평일/주말/월간 달성률을 O(원형 채움)으로, 33% 미만 빨강·66% 미만
  노랑·그 이상 초록으로만 구분하고 숫자는 아예 표시하지 말 것을 요청.
- 반영
  - `ui.js`: `subjectStatsListHTML`의 막대(`mini-meter`) 3개를 `progressRingHTML` 기반
    원형 프로그레스 3개(평일/주말/월간) 한 줄로 교체. 색상은 새로 만들지 않고 시험
    준비 캘린더에 이미 있던 상태색 `--cal-good`/`--cal-warn`/`--cal-bad`를 그대로 재사용
    (33%/66% 임계값도 같은 기준으로 통일). 목표가 아예 없는 항목(`rate === null`)은
    색이 채워진 호 없이 빈 트랙(회색 원)만 그려 "0%"와 구분.
  - SVG `<circle>` + `stroke-dasharray`/`stroke-dashoffset`로 원형 채움 구현(숫자 라벨 없음,
    과목/기간 이름만 텍스트로 남김). `css/style.css`에 `.ring-row`/`.ring-item`/`.ring-label`
    추가.
  - `service-worker.js` 캐시 버전 `v12` → `v13`.
- 확인: 임시 정적 서버 + Claude in Chrome에서 `localStorage`에 빨강(17%)·초록(83%)·
  노랑(50%)·목표없음(빈 링) 4가지 케이스를 만드는 시드 데이터를 넣고 대시보드를
  스크린샷/확대해 색상·채움 비율·빈 링 처리가 모두 의도대로 나오는지 확인.
- 15~16번 작업 후 커밋 1개(이전 세션 미푸시 커밋 포함 총 2개)를 `origin/main`에 push해
  GitHub Pages에 실제로 반영함.

### 17. iOS 홈 화면 아이콘 재추가로 인한 데이터 손실 + 복구
- 사고: 새 아이콘(15번)을 반영하려면 "기존 홈 화면 아이콘을 지우고 다시 추가"하라고
  안내했는데, 그 과정에서 사용자의 실제 공부 기록(`localStorage`)이 사라짐 — iOS가
  재추가된 홈 화면 웹앱에 이전 것과 분리된 새 저장 공간을 주는 것으로 추정.
- 복구: `OneDrive\바탕 화면\cta-backup\cta-backup-2026-09-18.json`(12번 작업에서 만든
  개인 백업 저장소 폴더, 아이폰에도 OneDrive로 동기화돼 있음)을 아이폰 파일 앱에서 찾아
  CTA 설정 탭 "JSON 불러오기"로 복구. 단, 백업 시점(9/18 저녁) 이후 입력분은 복구 안 됨.
- 교훈: 홈 화면 아이콘 삭제/재추가처럼 iOS PWA 저장소에 영향을 줄 수 있는 안내를 하기
  전에는 먼저 "설정 탭에서 JSON 내보내기로 백업부터 받으세요"를 반드시 안내한다.

### 18. `ui.js` 화면 단위 파일로 분리
- 배경: 13~16번 작업을 거치며 `ui.js`가 약 570줄까지 불어나 `RULES.md`의 분리 기준
  (약 300줄)을 계속 넘긴 채 방치돼 있었음. 사용자가 이를 상기시켜 지금 분리를 진행.
- 반영: `js/ui/{shared,dashboard,log,goal,volume,subjects,settings}.js`로 화면별 렌더
  함수와 그 화면에서만 쓰는 헬퍼를 나눔. 여러 화면이 같이 쓰는 작은 헬퍼
  (`escapeHtml`/`rateText`/`clampPct`/`roundBadgeText`)는 `ui/shared.js`로. `js/ui.js`는
  각 화면 파일의 `render*` 함수를 재export하는 배럴로만 남겨서 `app.js`의
  `import ... from "./ui.js"`를 그대로 유지(호출부 변경 없음). `RULES.md` 파일 구조
  설명도 이 구조에 맞게 갱신. `service-worker.js`의 `APP_SHELL`에 새 `js/ui/*.js`
  파일들과 (그동안 빠져 있던) `icons/icon-180.png`를 추가하고 캐시 버전 `v13` → `v14`.
- 확인: 파일 분리 후 각 파일의 import/export가 기존 심볼과 정확히 맞는지 코드 리딩으로
  대조(순수 리팩터링이라 동작은 바뀌지 않아야 함).

### 19. 수량 기준 전면 개편 (시간 → 문제 수/인강 수, 주간 격자, 이월, 회독)
- 배경: 사용해보니 시간 목표는 "앉아 있던 시간"이라 실제 진도와 어긋난다는 문제 제기. 세무사 1차/2차
  수험 특성(연습서 문제은행, 인강, 객관식 문제집, 시험 임박 시 몰아하기)에 맞춰 목표를 수량으로
  잡도록 전체 재설계. 의견 교환과 샘플 화면(HTML 목업) 검토를 거쳐 확정 후 구현.
- 결정 사항(사용자 확인): 기본 목표는 수량 / 목표 = 세부 과목 × 단위 / 1차·2차 두 트랙(한 번에 하나만
  활성, 자동 전환 없음) / 주 격자(큰 네모=주, 7칸=일)에 휴식·복습 지정(1번 휴식→2번 복습(주 1일)→
  3번 원상복귀) / 이월은 앱을 열 때 묻고 초과분이 오래된 이월분부터 상환, 완료 시 소급 연한 초록 /
  집중 모드는 수동 토글 / 시험 점수는 별도 탭 + 과목별 꺾은선 / 공부기록은 월별 접기·펼치기 /
  과목 색은 이름 기준 12슬롯(초과 시 직접 선택) / 공휴일은 표시만(자동 휴식은 옵션, 기본 꺼짐).
- 구현
  - 데이터 모델 v2(`AGENTS.md` 4절 참고). **저장 키를 `cta-data-v2`로 새로 씀** — 이전 버전 키
    (`cta-study-tracker-data`)는 건드리지 않고 남겨 두며 설정에서 JSON으로 내보낼 수 있음. 이전 버전
    백업 JSON은 새 버전에서 불러올 수 없음(명확한 오류 메시지).
  - `stats.js`는 순수 계산만: 하루 상태, 이월 상환(초과분 → 오래된 이월분), 주간 쿼터, 월별 집계.
    그날 목표는 `dayTargets` 스냅샷으로 고정해 목표를 나중에 고쳐도 지난 날 판정이 안 바뀜.
  - 화면 5개(주간/오늘/시험 기록/공부기록/설정)로 재구성. 이전 대시보드·기록·목표·공부량·과목
    화면 파일 삭제. 시험 점수 그래프는 선 끝에 과목명·점수 표기.
  - 색: `--series-1..8`은 기존 dataviz 검증값 유지, `--series-9..12`와 상태색(이월/휴식/복습)은
    새로 추가(별도 검증은 하지 않음 — 필요하면 dataviz 스킬로 재검증).
  - 공휴일: 공공데이터포털 "한국천문연구원 특일 정보"(`getRestDeInfo`)로 2026~2027 46건을 받아
    `data/holidays.json` 생성(서비스워커 프리캐시 + 네트워크 우선 갱신). `.github/workflows/
    update-holidays.yml`이 월 1회 갱신 — **저장소 Secrets에 `DATA_GO_KR_KEY` 등록 필요**(아직
    미등록, 워크플로는 로컬에서 실행해 보지 못함). 인증키는 코드/저장소에 넣지 않음.
  - `service-worker.js` 캐시 `v14` → `v15`, 새 파일 목록 반영.
  - 앱을 쓰기 전까지 푼 양 반영: 설정의 과목별 "누적 푼 양" 입력 → 총 분량 기준으로 회독과 현재
    진행량으로 환산(`storage.js`의 `setCumulative`). `entries`를 만들지 않아 주간 통계에는 안 잡힘.
    총 분량/누적 입력 순서와 무관하게 같은 결과. 1차·2차 모두 입력 가능(진행중 트랙이 위).
  - 실기기(아이폰) 피드백 반영: 날짜 입력칸이 옆 칸을 밀어내던 문제(iOS Safari 날짜칸의 큰 기본
    최소 폭) → 그리드 열을 `minmax(0, 1fr)`로 바꾸고 `input[type=date]`에 `appearance:none;
    min-width:0` 적용. 설정의 총 분량 카드에서 과목 이름·단위(책/강의 이름)를 바로 수정 가능하게 함
    (하루 목표·요일·삭제는 오늘 탭 편집). 캐시 `v16`.
  - 목표 회독 + 권장량 역산 추가(사용자 지적: 주간 분량은 목표 회독 × 총 문제 수에서 역산해야
    함). `stats.js`의 `paceFor`, 설정의 "목표 회독" 입력과 [적용] 버튼. 목표 자동 덮어쓰기는 하지
    않음(이월과 이중 계산 방지, AGENTS.md 참고).
  - 직장 병행 반영(사용자 요청): 하루 목표를 평일/주말 두 값으로 분리(`dailyTarget` →
    `weekdayTarget`/`weekendTarget`, `normalize()`가 옛 값을 두 곳에 이어받음), 공휴일은 주말 취급,
    권장량은 시험 30일 전 마감 + 평일:주말 = 공부 가능 시간 비율(4h:7h), 목표별 1개당 소요 시간
    (문제 20분/인강 60분)으로 오늘 탭 과부하 경고와 설정의 요일별 예상 시간 점검, 요일 패턴 프리셋
    3종(매일 전 과목/격일 묶음/추천 조합, 오늘 탭 편집). 검증: 총 1200 / 누적 340 / 목표 3회독 /
    시험일 2027-08-14 → 남은 3260, 7/15 마감, 평일 9 · 주말 16문제. (임의 수치로 7과목 전부 3회독을
    잡으면 평일 13시간 넘게 필요하다고 경고가 뜨는 것도 확인.)
  - `storage.js`(365줄)와 `app.js`(357줄)가 300줄 기준을 넘어 분리: `store.js`(저장·정규화),
    `goals.js`(목표·기록 변경), `actions.js`(클릭 처리), `inputs.js`(폼·입력 처리). app.js의 import는
    `storage.js` 하나로 유지. 분리 후 오늘 입력/되돌리기, 휴식·복습, 이월 창, 시험 기록, 설정 입력,
    프리셋, 권장량 일괄 적용, 내보내기를 다시 돌려 오류 없음 확인. 서비스워커 캐시 `v16`.
  - 오늘 입력 되돌리기: 직전 1건만 기억하던 것을 "오늘 입력한 기록을 최신 것부터 계속 취소"로 변경
    (`goals.js`의 `removeLastEntryOn`). 버튼에 건수 표시, 직전 입력 안내 문구.
  - GitHub 동기화(사용자 요청: 백업 데이터를 깃허브에 올리고 열 때마다 받기): `sync.js`, `ui/syncui.js`.
    저장소는 비공개 `chopu9377/cat-backup`의 `cta-data.json`, fine-grained 토큰(그 저장소 Contents
    읽기/쓰기). 열 때·복귀할 때 맞추고 변경 4초 뒤 자동 업로드, 초기 상태는 GitHub 우선, 양쪽이 다 바뀌면
    묻기(AGENTS.md 5-1 참고). `persist({quiet})`/`setChangeListener`로 "사용자 변경"만 알림(하루 스냅샷은
    제외), `meta.updatedAt` 추가. 서비스워커는 api.github.com 응답을 캐시하지 않음, 캐시 `v17`.
    검증은 GitHub API를 흉내 낸 가짜 서버(sha 검사 포함)로: 최초 올리기, 변경 후 재올리기, 기기 데이터
    삭제 후 복구, 충돌(GitHub 것/이 기기 것/나중에), 토큰 오류, 오프라인 후 복구, 토큰이 올라가는
    파일에 안 섞임까지 확인. **실제 GitHub 호출은 사용자 토큰이 필요해 아직 실행해 보지 못함.**
  - 설정의 과목 카드에 삭제(✕) 추가: 그동안 삭제는 오늘 탭 편집(진행 중 트랙만)에만 있어 1차 과목을
    정리할 수 없었음. 사용자가 1차 세법을 "세법학개론(객관식)" 하나로 합치려는 요청으로 발견.
    (이름/단위 수정 + ✕로 나머지 삭제 → 1차 9개 → 5개, 삭제한 목표는 보관 처리되어 기록 유지.)
  - 앱 업데이트가 폰에 늦게 보이는 문제(푸시 후 2분 넘게 옛 화면): 배포는 끝났지만(GitHub Pages
    빌드 성공·서버의 service-worker.js 버전 확인) iOS 서비스워커가 캐시된 옛 파일을 먼저 보여 주고
    새 버전은 다음 실행에 바뀜. 대응: 새 서비스워커가 활성화되면 한 번 자동 새로고침(첫 설치 제외),
    설정 하단에 앱 버전(캐시 이름 vN) 표시. 캐시 `v18`. 새 버전을 확인하려면 앱을 완전히 종료 후
    다시 열기(두 번). **자동 새로고침은 실제 업데이트 경로에서는 아직 검증 못 함**(로컬에서는 첫 설치
    시 새로고침이 반복되지 않는 것만 확인).
- 확인: 임시 정적 서버 + Claude in Chrome으로 시드 데이터(달성/부분/미달/휴식/복습/초과분)를 넣고
  이월 창 → 확정 → 초과분 상환(재무회계 3개 상환·나머지 대기), 회독 롤오버/되돌리기, 휴식→복습
  순환과 주 1일 제한, 집중 모드, 1차↔2차 전환, 새 과목 추가·12색 소진 후 직접 색 선택 폴백,
  공휴일 빨간 점 표시, 월별 접기/펼치기, 시험 점수 그래프를 확인. 콘솔 오류 없음.
  (테스트 중 서비스워커가 옛 파일을 캐시로 돌려줘 수정이 안 보이는 일이 있었음 — 로컬 테스트 시
  서비스워커/캐시를 지우고 확인할 것.)
- 미확인/남은 것: 실제 아이폰에서의 0~10 스크롤 선택 감촉, 라이트/다크 전체 화면 육안 점검,
  GitHub Actions 공휴일 갱신 실제 실행, 이월 확인 창을 "하루 첫 접속"이 아니라 "앱을 새로 열 때"
  기준으로 띄우는 점(백그라운드 복귀 시에는 안 뜸).

## 인수인계 메모 (다른 AI/도구가 이어서 작업할 때)

- 먼저 읽을 것: `AGENTS.md`(목적/기능), `RULES.md`(코딩 규칙), 이 파일(작업 이력), `CHANGELOG.md`(버전별
  변경 요약, 배포할 때마다 맨 위에 추가).
- **저장 데이터 보호가 최우선.** 사용자의 실제 기록이 아이폰 `localStorage`에 들어 있다.
  - 데이터 구조(필드)를 바꿀 때는 반드시 `storage.js`의 `normalize()`를 거치게 하고,
    누락된 필드는 기본값으로 채운다. `schemaVersion`이 다르다고 데이터를 초기화하지 않는다.
  - 필드 추가는 additive(기존 데이터가 그대로 동작)로 한다. 삭제된 필드는 무시만 하고 굳이
    지우지 않는다.
  - 백업 JSON 내보내기/가져오기는 저장 포맷과 1:1 전체 덤프를 유지한다.
- 자동 테스트가 없다. 수정 후에는 브라우저(모바일 폭 390~430px, 라이트/다크 둘 다)에서
  시드 데이터로 직접 확인한다. 특히 `stats.js` 계산(평일/주말/월간 달성률) 결과를 눈으로 검증.
- 색상 팔레트(파란 accent, 카테고리 8색, 상태색)는 CSS 커스텀 프로퍼티 값을 그대로 유지한다.
  이 값은 Claude Code의 dataviz 스킬로 검증된 것이므로, 다른 도구로 수정할 때는 바꾸지 말고
  모양·그림자·타이포만 조정한다.
- `ui.js`는 화면별 파일(`js/ui/*.js`)의 렌더 함수를 재export하는 배럴이다(19번 작업에서 화면 구성이
  주간/오늘/시험 기록/공부기록/설정으로 바뀜). 새 화면을 추가할 때도 같은 패턴(화면별 파일 +
  `ui.js`에 한 줄 추가)을 따른다. 상태 계산은 전부 `stats.js`(순수 함수)에 두고 화면은 그 결과만 그린다.
- 위 "저장 데이터 보호" 규칙에서 `subjects/logs`는 옛 이름이다. 지금은 `goals/entries` 기준이며
  저장 키는 `cta-data-v2`다(19번).
- 배포는 `git push`만으로 GitHub Pages에 반영된다. 서비스워커 캐시를 쓰므로, 정적 파일을
  바꾸면 `service-worker.js`의 캐시 버전도 함께 올려야 아이폰에 반영된다.

## TODO (다음에 할 일)
- [x] GitHub Pages 배포
- [x] 실제 아이폰에서 홈 화면에 추가해 실사용 테스트
- [x] 로컬 git → GitHub 인증(push) 방법 마련 — 사용자 PC의 일반 터미널에서 1회 로그인 후 해결
- [x] 개인 데이터 백업용 private 저장소 분리 (`cat-backup`)
- [x] 수량 기준 전면 개편 + 시험 점수 기록 (19번)
- [x] 개편본 커밋/푸시 및 GitHub Pages 반영 (사용자가 아이폰에서 확인하고 피드백을 줌)
- [x] `DATA_GO_KR_KEY` Secret 등록 + "Update holidays" 워크플로 수동 실행 → 성공(초록 체크)
- [x] 실제 아이폰 사용 점검: 날짜 입력칸이 옆 칸을 밀어내던 문제는 수정(v16). 스크롤 입력·주간 격자는
      사용 중이며 별도 불만 없음
- [x] 클라우드 동기화 → GitHub 동기화로 구현(v17)
- [x] 업데이트 내역 파일(`CHANGELOG.md`) 추가, 배포 시 갱신 규칙을 RULES.md에 명시
- [ ] **GitHub 동기화 첫 연결 결과 확인**: 사용자가 토큰까지 저장함. `cat-backup`에 `cta-data.json`과
      "CTA sync" 커밋이 생겼는지, 설정 카드 상태가 "동기화됨"인지 미확인(가짜 서버로만 검증했음)
- [ ] 서비스워커 **자동 새로고침**을 실제 업데이트(v18 → 다음 버전)에서 검증. 로컬에서는 첫 설치 때
      새로고침이 반복되지 않는 것만 확인함
- [ ] 사용자 입력 대기: 시험일(1·2차), 과목별 총 분량·누적·목표 회독. 입력 후 설정의 요일별 점검
      카드로 권장량이 무리 없는지 확인하고 필요하면 프리셋/목표 회독 조정
- [ ] 1차 활성 시작일(기본 12/15)에 1차로 전환할 때 1차 과목·목표 설정 점검(세법은 "세법학개론" 하나로
      정리할 예정)
- [ ] (선택) 워크플로 `actions/checkout@v4` → `@v5`(Node 20 폐기 경고, 동작에는 영향 없음)
- [ ] (선택) 앱 안에서 CHANGELOG 보기 화면
- [ ] (선택) 공부 시간 타이머를 보조 지표로 다시 붙일지 결정
- [ ] 알림 등 백로그는 필요 시 재논의
