# AGENTS.md — 공부 일정 기록표 (Study Tracker)

## 1. 목적

내가 공부한 날짜/과목/내용을 기록하면, 이를 바탕으로 목표 대비 달성률(%)과 교재별
회독수를 한눈에 볼 수 있는 개인용 학습 트래킹 앱을 만든다.

- 사용자: 1인 (본인 전용, 로그인/공유 기능 없음)
- 사용 기기: 아이폰 (홈 화면에 추가해 앱처럼 사용하는 PWA)
- 사용 빈도: 매일 기록하는 것을 전제로 설계

## 2. 핵심 기능 (v1 범위)

### 2.1 기록 입력
- 날짜, 과목, 공부한 내용(자유 텍스트), 공부 시간(분/시간)을 입력
- 선택적으로 어떤 교재(교재 단위 관리 시)를 공부했는지 연결

### 2.2 과목 & 교재 관리
- 과목(예: 수학, 영어)을 등록/삭제
- 과목마다 **평일 목표시간**(한 주간 평일 합계)과 **주말 목표시간**(한 주간 주말 합계)을 설정.
  월간 목표는 별도로 입력받지 않고 `(평일 목표 + 주말 목표) × 4주`로 자동 계산한다
  (직장 병행 등으로 평일/주말 공부량 차이가 커서, 매번 월간 목표를 따로 손으로 맞추지
  않아도 되게 하기 위함).
- 과목 아래에 교재(예: "수학정석", "개념원리")를 등록
- 교재마다 "N회독 완료" 버튼으로 회독수를 수동 증가 (진도율 자동 판정 없음)
- 교재별 회독 이력(몇 회독을 언제 완료했는지) 조회
- 교재마다 목표 회독수(선택)를 설정할 수 있고, 설정 시 "n/목표 회독" 진행률 바로 표시

### 2.3 통계 / 대시보드
- **이번 주 평일 달성률(%)** = 이번 주 평일(월~금) 실제 공부시간 / 평일 목표시간
  (과목별 + 전체 합산)
- **이번 주말 달성률(%)** = 이번 주 토·일 실제 공부시간 / 주말 목표시간 (과목별 + 전체 합산).
  평일/주말을 완전히 독립된 목표로 두되(토+일 합산 기준, 요일별 개별 목표는 아님), 계산
  방식 자체는 동일한 원리.
- **이번 달 달성률(%)** = 이번 달 실제 공부시간 / 자동 계산된 월간 목표
  (`(평일 목표 + 주말 목표) × 4`)
- 누적 총계: 시작일부터 지금까지의 총 공부시간, 누적 목표 대비 누적 달성률
- 과목별 공부시간 분포 (어떤 과목에 얼마나 시간을 썼는지)
- 교재별 회독수 현황
- 과목의 평일/주말 목표는 과목 추가 시점뿐 아니라 이후에도 '과목' 탭에서 언제든 수정 가능

### 2.4 백업 / 복원
- 전체 데이터를 JSON 파일로 내보내기(다운로드)
- JSON 파일을 불러와서 데이터 복원(가져오기)
- 이유: iOS Safari의 PWA는 오래 사용하지 않으면 로컬 저장소가 삭제될 수 있음
  (RULES.md 참고). 수동 백업이 유일한 안전망이므로 필수 기능으로 취급한다.

### 2.5 시험 준비 보조 기능
- **D-day 카운터**: 설정 화면에서 시험 이름·시험일을 등록하면 대시보드 상단에
  "{시험 이름} D-{남은 일수}" 배너로 표시.
- **연속 학습일(스트릭)**: 오늘 또는 어제까지 하루도 빠짐없이 기록한 날수를 대시보드에 표시.
  장기전인 시험 특성상 꾸준함 유지 동기부여 목적.
- **기록 태그(오답노트/취약 유형)**: 기록 입력 시 쉼표로 구분한 태그(예: "계산실수",
  "조문미숙")를 선택적으로 남길 수 있고, 기록 탭에서 태그로 필터링해 모아볼 수 있음.
- **하루 목표 시간 + 시험 준비 캘린더**: 설정에서 하루 목표 시간(전체 과목 합산 기준)을
  정하면, 대시보드에 오늘부터 시험일까지 날짜를 ㅁ 모양 셀로 나열한 캘린더가 표시됨.
  그날 목표를 채웠으면 초록, 일부만 했으면 노랑, 아예 안 했으면 빨강, 아직 오지 않은
  날은 회색으로 표시 (최대 120일까지 표시, 그 이상은 앞부분만 보여주고 총 D-day는
  배너로 확인).

## 3. 데이터 모델 (개념 설계)

```
Subject (과목)
  - id, name
  - weeklyGoalMinutes (평일 목표, 한 주 기준)
  - weekendGoalMinutes (주말 목표, 한 주 기준)
  - materials: Material[]
  // 월간 목표는 저장하지 않고 (weeklyGoalMinutes + weekendGoalMinutes) x 4 로 매번 계산

Material (교재)
  - id, subjectId, name
  - roundHistory: { roundNumber, completedAt }[]   // 회독 완료 이력

StudyLog (공부 기록)
  - id, date, subjectId, materialId? (선택)
  - durationMinutes
  - content (자유 텍스트: 무엇을 공부했는지)
  - tags: string[] (선택, 취약 유형/오답 태그)

Meta (앱 전역 정보)
  - lastBackupAt
  - examName, examDate (D-day 카운터·캘린더용)
  - dailyGoalMinutes (하루 목표 시간, 시험 준비 캘린더 색상 판정 기준)
```

정확한 필드/타입은 구현 시 RULES.md의 데이터 저장 규칙을 따른다.

## 4. 화면 (산출물) 목록

1. **기록 추가 화면** — 오늘 날짜 기본값, 과목 선택, 내용 입력, 시간 입력
2. **대시보드 화면** — 이번 주 %, 이번 달 %, 누적 현황, 과목별 분포 (진입 시 기본 화면)
3. **과목/교재 관리 화면** — 과목·교재 추가/삭제, 목표시간 설정, 회독 완료 버튼
4. **기록 히스토리 화면** — 날짜별 지난 기록 목록 조회/수정/삭제
5. **백업 화면** — JSON 내보내기/가져오기
6. **설정 화면** (필요시 3, 5와 통합 가능)

## 5. 비목표 (v1에서 하지 않는 것)

- 다중 사용자, 로그인/인증
- 클라우드 동기화 (추후 확장 여지는 열어두되 v1에는 미포함)
- 네이티브 앱 / App Store 배포
- 알림·리마인더 기능
- 회독 진행률 자동 판정 (항상 사용자가 수동으로 "완료" 클릭)

## 6. 향후 확장 아이디어 (백로그, 확정 아님)

- 클라우드 DB(Firebase/Supabase) 연동으로 기기 간 동기화
- 공부 알림/리마인더
- 주/월 목표 달성 히스토리 그래프

## 7. 앱 기본 정보

- 앱 이름: **CTA**
- 테마 색상: 파란 계열
- 다크모드: 지원 (자동 — 기기 라이트/다크 설정을 따름)
- 배포(호스팅): GitHub Pages
