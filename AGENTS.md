# AGENTS.md — 공부 일정 기록표 (Study Tracker)

## 1. 목적

세무사 시험(1차/2차) 수험생인 사용자가 **하루 목표량을 수량(문제 수, 인강 수 등)으로 세우고**
기록하면, 주간 달성 현황·이월·회독·시험 점수 추이를 한눈에 볼 수 있는 개인용 학습 트래킹 앱.
(2026-09 개편: 시간(분) 기준 → 수량 기준. 옛 설계는 WORKLOG.md 참고.)

- 사용자: 1인 (본인 전용, 로그인/공유 기능 없음)
- 사용 기기: 아이폰 (홈 화면에 추가해 앱처럼 사용하는 PWA)
- 사용 빈도: 매일 기록하는 것을 전제로 설계

## 2. 핵심 개념

- **트랙**: 1차 / 2차. 한 번에 하나만 활성. 전환 시 반대 트랙은 일시정지(진도 보존).
  자동 전환은 하지 않는다 — 1차 활성 시작일이 지나면 "1차로 전환할까요?", 1차로 전환해 둔 상태에서 1차 시험일이
  지나면 "2차로 돌아갈까요?" 안내만 띄운다(안내는 활성 시작일~시험일 사이/시험 다음 날부터만 뜬다).
- **목표(goal)**: `세부 학습 과목 × 단위` 하나. 예) 재무회계 / 연습서 문제, 세법학 / 인강(강).
  하루 목표량(평일/주말), 한 주에 며칠 할지(`daysPerWeek`, 요일은 매주 랜덤 배치가 정함), 총 분량(`total`),
  회독(`round`)과 현재 회독 진행량(`progress`)을 가진다. 이름·단위는 자유롭게 수정 가능.
- **과목 색**: 이름 기준(같은 과목이면 트랙과 상관없이 같은 색). 기본 12색 슬롯을 먼저 배정하고,
  12개를 넘으면 사용자가 직접 색을 고른다.
- **하루 상태**: 그날 목표를 전부 채우면 당일 달성(진한 초록). 일부만 하면 부분(노랑), 안 했으면
  미달(회색). 사용자가 미래(오늘 포함) 날짜를 탭해 휴식(1번) → 복습(2번, 주 1일만) → 원상복귀(3번).
  휴식·복습일은 목표가 없고, 복습일은 진도를 빼지 않는 "회상(recall) 날"이다.
- **이월(carry)**: 고정 목표는 앱을 열 때 전날까지 미달분이 있으면 이월/버림을 묻는다(밀린 날은 묶어서).
  이월하면 그날 칸이 이월 대기(주황 점선)가 되고, 이후 **그날 목표를 넘겨 푼 초과분**이 이월분을
  오래된 순서로 갚는다(당기분 먼저, 이월결손금 공제와 같은 순서). 다 갚으면 그날 칸이 연한
  초록(이월 후 완료)으로 소급 변경되고, 그 주 전체가 완료되면 주 카드가 초록이 된다.
  **자동 목표는 묻지 않고 그 주 안에서 자동 소급**한다(`autoDebts`): 지난 날 못 채운 양이 부족분이 되고(칸은 이월 대기),
  그날 목표를 넘겨 푼 양(휴식·복습일에 푼 것 포함)이 먼저 생긴 부족분부터 갚는다. 하루 목표에 이월분을 얹지 않고 오늘 탭 "이월 N" 뱃지로
  보여 준다. 주가 끝나면 못 갚은 부족분은 그 칸이 부분/미달로 굳고 양은 다음 주 역산에 이미 들어가므로 다음 주 초과로는 갚지 않는다
  (두 번 세지 않기 위해). 소급에 쓴 초과분은 휴식 저축에 쌓이지 않는다. **채우기 목표도 자동 목표와 똑같이** 묻지 않고 그 주 안에서 자동 소급한다(v44).
- **오늘 입력 되돌리기**: 오늘 입력한 기록을 최신 것부터 하나씩 계속 취소할 수 있다(횟수 제한 없음,
  앱을 다시 열어도 유지).
- **새벽 모드**: 자정~새벽 7시 사이에 앱을 열면 "오늘"이 실제 달력 날짜가 아니라 **어제 목표가 끝났는지**로
  정해진다 — 전날 목표가 아직 안 끝났으면(미달·이월 미결정) 여전히 전날로 보고, 전날이 이미 달성·휴식·복습·
  목표 없음이었으면 그냥 오늘로 본다. 새벽까지 이어서 공부하거나 반대로 새벽에 일찍 일어나 그날치를 새로
  시작하는 경우를 자동으로 구분하기 위함이다. 한 번 정해지면 그 새벽 동안은 고정(기록을 더 채워도 안 바뀜)
  되고, 오늘 탭 상단 배너의 [바꾸기]로만 다른 날짜로 넘길 수 있다(둘 중 하나 선택). 7시가 지나면 새벽 모드가
  풀리고 실제 오늘로 돌아간다. 이 선택은 기기별 localStorage(`cta-dawn`)에만 남고 동기화되지 않는다.
- **회독**: 현재 회독 진행량이 총 분량에 도달하면 회독 +1, 진행량은 0(넘친 만큼 이월).
  앱을 쓰기 전까지 푼 양은 설정의 "누적 푼 양"에 넣으면 총 분량 기준으로 회독/진행량으로 환산된다
  (주간 통계에는 잡히지 않음).
- **평일/주말**: 토·일과 **공휴일은 주말**로 본다(공부 가능 시간이 더 길다). 고정 목표는 하루 목표를 평일 값과 주말 값 두 개로 갖는다(직장 병행).
- **목표 회독 → 역산**: 목표마다 목표 회독수를 두면 `(목표 회독 × 총 분량 − 지금까지 푼 양)`을 **시험일 N일 전(기본 30일,
  마지막 기간은 모의고사·복습)까지** 끝내도록 자동 계획이 이번 주 필요량을 구한다. 평일:주말 비율은 공부 가능 시간 비율(기본 4h:7h ≈ 1:1.75).
  공부일수는 휴식·복습일을 뺀 날 × 주 N일/7. 설정의 [권장량 도출]이 무엇이 비었는지, 과목별 이번 주 배분·예상 완료일·상태,
  이번 주 요일별 예상 공부 시간, 트랙 전체 가능 여부를 보여준다.
- **하루 목표 모드(고정 / 자동 / 채우기)**: 목표마다 `planMode`를 고른다(설정의 과목 카드 "하루 목표"). **고정**은 평일/주말 숫자를 그대로 쓴다. 새 목표의 기본값은 자동이고 [권장량 도출]의 [모든 과목 자동으로]로 고정 목표를 한 번에 바꾼다.
  **채우기**는 역산하지 않고 다른 과목을 배치한 뒤 그날 공부 가능 시간까지 남는 자리를 하루 최대 2개씩 채운다(세법학 인강처럼
  총량이 미정이고 최대한 빨리 끝내는 과목용). 총 분량이 있으면 남은 양까지만 채우고, 못 채운 날은 이월을 묻지 않는다.
  **자동**은 매주 시작 시점(켠 주는 켠 날)의 진도로 이번 주 필요량을 소수까지 구한 뒤 정수로 랜덤 배치된 날에 나눠 그 주 동안 고정한다
  (`남은 분량 = 총분량 × 목표회독 - 실제 누적진도`, 시험일·총 분량·목표 회독이 없으면 고정 목표를 쓴다). 실제로 하지 않은 문제를
  회독 완료로 세지 않으며 자동 목표에는 "버림/건너뛰기"가 없다. **미달은 같은 주 안에서만 소급**되고(위 이월 항목),
  그 주에 못 갚은 미달은 다음 주 재역산에 자동 흡수된다(주 초에 안내). 계획은 저장하지
  않고 기존 데이터로 매번 다시 계산하므로 기기끼리 결과가 같다. 계획에 영향을 주는 변경(총 분량·회독·누적·주 N일·평일/주말 양·시험일·공부 시간·
  이번 주 휴식/복습일·과목 추가/삭제)을 하면 모든 자동 목표의 `replanFrom`과 `layoutFrom`을 그 날짜로 두고, 그날부터 남은 날을 그날 진도 기준으로
  다시 나누고 다시 섞는다(지난 날은 굳어 있다. 그 주에 이미 생긴 부족분은 다시 나눠도 없어지지 않고 주가 끝날 때까지 이월 대기로 남아
초과분으로 갚으며, 주간 목표도 지난 날은 그날 목표 스냅샷으로 센다. 주 중간에 자동을 켠 경우만 켜기 전 날을 실제 한 양으로 센다).
- **주간 랜덤 배치**(`js/layout.js`): 과목별 요일을 고정하지 않고 매주 랜덤으로 정한다(공부 권태기 대응 — "내일은 무슨 과목이지?").
  과목마다 한 주 양(자동은 역산 필요량, 고정은 평일/주말 숫자 × 주 N일)과 주 N일은 그대로 두고, 요일별 시간이 공부 가능 시간 비율에
  가까운 배치들(최선과 거의 같은 후보) 중 하나를 고른다. **과목이 3일 넘게 비는 배치는 사실상 금지**(주 1일짜리는 예외, 휴식·복습일은 빈 날로 세지 않음),
  **이틀 연속 과목이 2/3 이상 겹치면 감점**(완전히 같으면 더 크게), 채우기 과목은 마지막에 남는 시간에 넣는다. 보상 휴식을 걸어 둔 과목은 그날에서 빠지지 않는다.
  시드는 트랙·주 시작일·재배치 기준일(`layoutFrom`)·공부일 목록이라 저장 없이 기기끼리 같은 결과가 나오고, 기록을 더 해도 그 주 배치는 안 바뀐다.
  휴식일을 지정·해제하면 그날부터 남은 날만 다시 섞이고(지난 날은 `dayTargets` 스냅샷), 되돌리면 원래 배치로 돌아온다. 다음 주보다 먼 주는 미리보기다.
- **하루 구성 모드**(설정 › 권장량 도출 "하루 구성", `settings.layoutMode`): **기본**(과목별 주 N일) / **진득**(매일 회계[재무·원가] 하나 +
  세무회계[법인·소득·부가] 하나를 묵직하게 — 묶음 자동 과목은 주 N일 대신 며칠에 나눌지도 배치가 정하고, 묶음마다 하루 한 과목에서 벗어나면
  감점해 시간 균형이 크게 깨질 때만 1·3과목을 허용. 세법학 등 묶음 밖 과목은 주 N일 그대로) / **물붓기**(자동 과목을 남은 공부일 전부에
  조금씩 — 시험 직전 감 유지용. 고정·채우기 과목은 그대로). `settings.autoSpreadDays`(기본 60, 0이면 끔)만큼 그 트랙 시험일이 남으면
  자동으로 물붓기가 된다. 모드는 그 주 재배치 기준일에 정해지고, 바꾸면 오늘부터 남은 날이 다시 섞인다. 묶음은 `presets.js`의 `DEEP_GROUPS`(과목 이름 기준).
- **1차 집중 기간 제외**: 1차 활성 시작일 ~ 1차 시험일 전날(둘 다 입력돼 있어야 계획으로 인정)은 1차만
  공부하는 기간이라 **2차 권장량의 공부일수에서 뺀다**(1차 시험이 끝나면 다시 2차 기간).
- **휴식 저축(보상 휴식)**: 그날 목표를 넘겨 푼 양(초과분, 목표가 없던 날·휴식일에 푼 것 포함)을 **과목별로**(개수) 모은다.
  그 과목 이월분을 먼저 갚고 남은 초과만 쌓이며(당기분 우선, 기존 이월 상환 규칙 그대로), 상한은 **과목마다 7시간어치**(개수 × 1개당 소요 시간,
  원기옥). 저축은 **그 과목 목표만** 줄일 수 있다 — 세법학을 더 풀어 모은 저축으로 법인세를 미루지 못하게(다른 과목을 미루면 그 과목 뒷날이
  무거워진다). 같은 과목 안에서는 먼저 푼 양을 나중 목표에서 빼는 것이라 총량이 그대로다(줄인 날 안 한 양은 진도에 안 잡혀 다음 주 계획에
  다시 들어가고, 먼저 푼 초과분이 낮춰 둔 계획과 상쇄된다). 상한을 넘는 초과는 저축에 안 쌓이고 진도로만 남아 다음 주 목표를 낮춘다.
  주간 탭에 과목별 저축 게이지("법인세 2문제 (40분)")가 뜨고, [저축 쓰기] → 날짜 칸 선택(달을 넘겨 미래 어느 날이든 가능) → **과목마다**
  스테퍼로 얼마나 줄일지 고른다(그날 그 과목 목표와 그 과목 저축 중 작은 쪽까지). 그날 과목 중 저축이 있는 과목이 하나도 없으면 고를 수 없다.
  **시험일 3주 전부터의 날짜는 회색으로 잠겨 쓸 수 없고**, 지난 날·휴식/복습일·이미 보상 휴식인 날·배치된 목표가 없는 날도 고를 수 없다.
  🎁 칸을 탭하면 취소(줄인 양이 그 과목 저축으로 돌아옴). 저장은 사용 기록 `bonusRest{ 날짜: { 목표id: 줄인 양 } }`뿐이고(옛 기록은 `날짜: 분` 숫자
  하나로, 비율 차감으로 그대로 읽히지만 과목을 알 수 없어 어느 과목 저축에서도 빼지 않는다) 저축액은 기록에서 매번 다시 계산한다.
- **진행 상태(🐢🚗🚀)**: 주간 탭 맨 위 카드는 **이번 주 계획 대비**(🐢 미흡: 어제까지 계획보다 뒤처짐 / 🚗 순항 / 🚀 초고속: 오늘 목표까지
  채우고도 초과)이고, 진도율 탭 과목 옆 뱃지는 **총 진도**(예상 완료일 vs 시험 마감: 위험·약간 밀림 → 🐢, 정상 → 🚗, 여유 → 🚀, 목표 회독
  완료 → 🏁)다. 설정의 [권장량 도출]에는 상태 표시를 넣지 않는다.
- **유지 모드**: 다른 트랙을 집중하는 동안 이 트랙을 가볍게 이어 가는 기능(트랙별 설정, 2차 기본 켜짐).
  목표마다 유지 평일/주말 양이 따로 있고 요일은 주 N일을 거의 같은 간격으로(시작 요일만 주마다 랜덤) 정한다. 오늘 탭의 "유지" 구역에서
  입력하며, **달성/미달 판정·이월·쿼터에는 넣지 않고**(못 채워도 괜찮음) 예상 공부 시간(과부하 경고)과 회독
  진행에는 포함한다. 권장량 계산에서는 **1차 집중 기간에 예정된 유지분을 진도로 인정**해 남은 분량에서 뺀다
  (유지량을 올리면 그 트랙의 권장량이 내려간다).
- **과부하 점검**: 목표마다 "1개당 소요 시간(분)"(문제 20분, 인강 60분 기본)을 두고, 오늘 목표 합계가
  그날 공부 가능 시간을 넘으면 오늘 탭에 경고, 설정에는 이번 주 랜덤 배치의 요일별 과목·예상 시간을 표시.
- **주 N일**(오늘 탭 편집): 과목마다 요일 칩 대신 "주 N일" 스테퍼(1~7)를 둔다. 요일 패턴 프리셋은 랜덤 배치로 대체되어 없앴다.
  새로 시작하는 기본 목표의 주 N일은 예전 추천 조합 요일 개수(회계·세무회계 3일, 세법학 인강 5일, 세법학 문제 1일)를 따른다.
  옛 데이터는 켜 두었던 요일 개수를 이어받고, `weekdays`는 옛 버전 호환용으로만 남는다.
- **공휴일**: `data/holidays.json`(정기 작업이 공공데이터포털 API에서 갱신)을 읽어 격자에 빨간 점으로
  표시. 설정의 "공휴일 자동 휴식"을 켜면 공휴일을 휴식일로 처리(기본 꺼짐).

## 3. 화면 (탭 6개)

1. **주간** — 트랙 전환 + D-day, 이번 달에 걸친 주간 격자(큰 네모=주, 7칸=일), 이번 주 쿼터
   링(과목별 색, 채워질수록 진해지고 100%면 ✓, 주간 목표를 넘기면 ★ + "훌륭 (초과!)"). 이월 미결정 알림·지난주 미달 자동 반영 안내.
   맨 위에 이번 주 진행 배지(🐢🚗🚀)와 휴식 저축 게이지·[저축 쓰기](날짜 고르기 모드에서는 달 이동 가능).
2. **오늘** — 오늘 목표 목록(과목 탭 → 탭바 위 입력 시트에서 0~10 스크롤 → +추가, 누적, 되돌리기). 오늘 목표 없는
   과목은 접혀 있다. 숫자에는 단위(문제·강·일)를 붙인다. 편집 모드에서 과목명·
   단위·하루 목표·주 N일 수정, 목표 추가/삭제(삭제해도 기록은 보존).
   맨 아래 [내일 미리보기]를 펼치면 내일 과목 수·예상 공부 시간·과부하를 보여 주고, 과목은 카드 뒷면(?)으로 가려 두었다가 탭하면 뒤집어
   보여 준다([모두 뒤집기], 뒤집은 상태는 화면 상태로만 기억). 지금 기록 기준이며 저장 값은 없다. 내일이 새 주의 첫날이면
   지금 진도 기준 "예상"이고, 오늘 못 채운 자동 과목이 있으면 이번 주 안에 더 풀면 자동으로 갚아진다고 알려 준다.
3. **진도율** — [2차]/[1차] 선택 → 과목(목표)별 현재 회독차(목표 회독이 있으면 `2/3`), 현재 회독 진행률 링, 진행량/총 분량,
   목표 회독까지 남은 양, 누적량. 진도 입력은 오늘 탭.
4. **시험 기록** — 1차/2차 선택 → 시험 과목 4개 점수 입력, 과목별 꺾은선 그래프(선 끝에 과목명·
   점수, 과목 켜고 끄기, 과락 40 / 평균 60 기준선 토글). 1차: 회계학·세법학·재정학·행정소송법,
   2차: 재무회계·원가관리회계·세무회계·세법학.
5. **공부기록** — 시작일부터 시험일까지 월별 카드(달성률만 보이고, 탭하면 주간 격자가 펼쳐짐).
6. **설정** — 시험일(추정 여부)·1차 활성 시작일, "권장량 도출"(공부 가능 시간·하루 구성 모드·자동 물붓기 시작 + 트랙 › 공통 조건 · 과목별 입력 상태·결과
   트리로 비어 있는 입력을 알려 줌), 과목별 총 분량/회독, 공휴일 옵션, 백업
   (내보내기/불러오기), 이전 버전(시간 기준) 데이터 내보내기.

## 4. 데이터 모델 (schemaVersion 2, localStorage 키 `cta-data-v2`)

```
startDate                 // 첫 주의 시작일(기본 2026-09-20). 주 = startDate 기준 7일
trackSwitches[]           // { from, track } 언제부터 어느 트랙이 활성이었는지
tracks[1|2]               // { examDate, examEstimated, activeFrom, maintain }
goals[]                   // { id, track, subject, unit, weekdayTarget, weekendTarget, maintWeekdayTarget, maintWeekendTarget, minutesPerUnit, daysPerWeek(1~7), weekdays[](옛 버전 호환용), total, targetRounds, round, progress, archived, planMode("fixed"|"auto"|"fill"), autoFrom, replanFrom }
layoutFrom                // 주간 랜덤 배치를 마지막으로 다시 섞은 날(계획 변경 시각). 그 주는 이날부터 남은 날만 다시 섞는다
entries[]                 // { id, goalId, date, amount }  푼 양(누적 입력 1건 = 1행)
dayKinds{ date: rest|review }
dayTargets{ date: { goalId: target } }   // 그날 목표 스냅샷(지난 날 판정이 목표 수정에 흔들리지 않게). 오늘 것은 앱을 열 때마다 한 번 새로 계산
bonusRest{ date: { goalId: amount } }    // 보상 휴식: 그날 과목별로 줄이기로 한 양(옛 기록은 date: minutes 숫자 하나, 비율 차감). 휴식 저축은 여기 + entries로 계산
settlements{ date: carried|dropped }     // 미달분 이월/버림 결정
carries[]                 // { id, goalId, fromDate, amount }  고정 목표의 이월분(상환은 entries에서 계산). 옛 버전의 redistribute 이월은 무시한다
                          // 자동 목표의 소급은 저장하지 않고 entries + dayTargets로 매번 계산(stats.buildContext → autoDebts)
exams{ 1|2: [{ id, date, label, scores[4] }] }
subjectColors{ 과목명: 슬롯번호(1~12) | "#rrggbb" }
settings{ holidayAutoRest, weekdayHours, weekendHours, bufferDays, layoutMode("basic"|"deep"|"spread"), autoSpreadDays }
meta{ lastBackupAt, updatedAt }   // updatedAt: 사용자가 마지막으로 바꾼 시각(동기화 판단용)
```

상태(달성/부분/이월 대기 등), 이월 상환, 주간 쿼터는 저장하지 않고 `stats.js`에서 항상 계산한다.
새벽 모드의 판정(그 새벽 동안 "오늘"을 어느 날짜로 볼지)은 기기별 localStorage `cta-dawn`(`{ real, choice }`)에만
남고 위 `cta-data-v2`에는 들어가지 않는다(백업/동기화 대상 아님).

## 5-1. GitHub 동기화
- 설정에서 저장소(기본 `chopu9377/cat-backup`, 비공개), 파일 경로(`cta-data.json`), fine-grained 토큰
  (그 저장소 Contents 읽기/쓰기만)을 넣으면 켜진다. 서버 없이 브라우저에서 GitHub Contents API를 직접 부른다.
- **앱을 열 때·앱으로 돌아올 때(30초 이상 지났으면)** GitHub와 맞추고, 기록을 바꾸면 4초 뒤 자동으로 올린다.
  백그라운드로 가면 대기 중인 변경을 바로 올리고, 오프라인이면 나중에 다시 시도한다.
- 판단 기준은 마지막으로 맞춘 파일 버전(sha)과 "올리지 않은 변경(dirty)" 여부:
  · 사용자가 한 번도 안 바꾼 초기 상태(새로 설치/데이터 손실)면 **GitHub 데이터를 항상 우선** 불러온다
    (빈 데이터가 정상 데이터를 덮어쓰는 사고 방지).
  · GitHub만 바뀌었으면 불러오고, 이 기기만 바뀌었으면 올린다.
  · **양쪽 다 바뀌면 사용자에게 묻는다**(이 기기 것/GitHub 것/나중에). GitHub는 커밋 기록으로 되돌릴 수 있다.
- 토큰과 저장소 설정은 앱 데이터와 다른 localStorage 키(`cta-sync`)에 저장되어 백업/동기화 파일에 섞이지 않는다.
  토큰은 `CTA`(앱 코드가 있는 공개 저장소)가 아니라 별도 비공개 저장소에만 권한을 줘야 한다.
- 서비스워커는 `api.github.com` 응답을 캐시하지 않는다.

## 5. 백업 / 복원
- 전체 데이터를 JSON으로 내보내기/불러오기 (저장 포맷과 1:1). iOS Safari PWA는 저장소가 삭제될 수
  있어(홈 화면 아이콘 재추가 시 실제로 발생) 수동 백업이 유일한 안전망이다.
- 이전 버전(`cta-study-tracker-data` 키) 데이터는 새 버전이 건드리지 않고 남겨 두며, 설정에서
  JSON으로 내보낼 수 있다. 이전 버전 백업 파일은 새 버전에서 불러올 수 없다(형식이 다름).

## 6. 비목표
- 다중 사용자, 로그인/인증, 클라우드 동기화, 알림, 네이티브 앱
- 자동 트랙 전환 (사용자가 직접 결정)
- 오답 태그·정답률 입력 (기록 부담이 커서 제외)

## 7. 앱 기본 정보
- 앱 이름: **CTA**, 테마: 파란 계열, 다크모드: 기기 설정 자동
- 배포(호스팅): GitHub Pages
