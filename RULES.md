# RULES.md — 코딩 규칙

목적/기능 정의는 AGENTS.md를 따른다. 이 문서는 "어떻게 만들 것인가"만 다룬다.

## 1. 기술 스택

- 순수 HTML / CSS / JavaScript(Vanilla JS)만 사용한다.
- 빌드 도구, 번들러, 프레임워크(React/Vue/Svelte 등)를 도입하지 않는다.
- npm/패키지 매니저 없이, 파일을 열거나 정적 호스팅에 올리는 것만으로 동작해야 한다.
- 외부 라이브러리가 꼭 필요하면 CDN `<script>` 태그로 최소한만 추가한다 (도입 전 사용자와 상의).
- 예외: 폰트는 jsdelivr CDN의 **Pretendard**(오픈소스, 토스 느낌의 모던한 국문 서체)를
  `<link rel="stylesheet">`로 불러온다. 오프라인에서는 자동으로 시스템 폰트로
  대체되도록 폰트 스택 마지막에 `system-ui` 등을 남겨둔다. 서비스워커 프리캐시
  목록에는 넣지 않는다 (설치 시 CDN 접근 실패로 전체 설치가 막히는 것을 방지).

## 2. PWA 구성

- `manifest.json`으로 앱 이름("CTA"), 아이콘, 테마 색상(파란 계열), `display: standalone` 정의
- `service-worker.js`로 정적 자원을 캐싱해 오프라인에서도 기록 입력이 가능하게 함
- 아이폰 홈 화면 추가 시 상태바/세이프 에어리어(`viewport-fit=cover`, `env(safe-area-inset-*)`) 대응
- 다크모드는 별도 토글 없이 `prefers-color-scheme` 미디어쿼리로 기기 설정을 자동 반영한다.
  색상 값은 CSS 커스텀 프로퍼티(`--bg`, `--text`, `--accent` 등)로 정의해 라이트/다크 두 세트를 관리한다.

## 3. 파일 구조

```
/index.html
/css/style.css
/js/
  app.js        # 진입점: 화면 전환/렌더, 클릭·스크롤 위임, 초기화
  actions.js    # data-action별 클릭 처리
  inputs.js     # 폼 제출·입력칸 변경 처리
  storage.js    # 저장 관련 함수의 창구(app이 여기서만 import). 날짜·설정·시험·백업 변경 + 아래 두 파일 재export
  store.js      # localStorage 읽기/쓰기, 기본값·정규화(normalize), 스냅샷
  goals.js      # 목표·기록(entries)·회독 변경
  sync.js       # GitHub 저장소 JSON과 동기화(브라우저에서 GitHub API 직접 호출, 토큰은 별도 키)
  stats.js      # 하루 상태·이월 상환·주간 쿼터·월별 집계 등 순수 계산 로직
  bonus.js      # 휴식 저축(보상 휴식)·이번 주 진행 상태(🐢🚗🚀) 계산(순수 함수)
  plan.js       # 권장량(회독 역산)·1차 집중 기간·유지 모드·요일별 부하 등 계획 계산(순수 함수)
  weekplan.js    # 주간 자동 역산 미리보기(주 필요량 → 정수 요일 배분, 실제 페이스, 트랙 가능 여부)
  version.js     # 저장 데이터 변경 번호(계산 캐시 무효화용)
  dates.js      # 날짜 문자열 헬퍼
  presets.js    # 트랙별 기본 목표, 시험 과목, 과목 색 슬롯
  holidays.js   # data/holidays.json 로드, 공휴일 이름 조회
  ui.js         # 화면별 렌더 함수를 다시 내보내는 배럴(barrel) 파일
  ui/
    shared.js     # 여러 화면이 같이 쓰는 작은 헬퍼(escapeHtml, 링 SVG, 과목 색 등)
    weekgrid.js   # 주 카드/범례 (주간·공부기록 화면 공용)
    week.js       # 주간 화면
    today.js      # 오늘 화면
    progress.js   # 진도율 화면(트랙별 과목 회독차·진행률 링)
    exam.js       # 시험 기록 화면
    history.js    # 공부기록 화면
    settings.js   # 설정 화면
    plantree.js   # 설정의 "권장량 도출" 트리(입력 상태·결과·요일별 예상 시간)
    bonus.js      # 주간 진행 카드·휴식 저축 사용/취소 시트
    tomorrow.js   # 오늘 탭 맨 아래 "내일 미리보기" 접이식 카드
    settle.js     # 이월 확인 하단 시트
    syncui.js     # 설정의 GitHub 동기화 카드, 동기화 충돌 선택 시트
/data/holidays.json          # 공휴일 목록(정기 작업이 갱신, 서비스워커가 미리 캐시)
/.github/workflows/          # 공휴일 갱신 작업(앱 코드가 아니라 저장소 자동화)
/manifest.json
/service-worker.js
/icons/
```

역할별로 파일을 나누되, 과도하게 쪼개지 않는다. 파일 하나가 너무 커지면(대략 300줄 이상)
그때 분리한다. `js/ui.js`가 여러 화면을 한 파일에 다 담아 300줄을 크게 넘겼던 적이 있어
`js/ui/` 밑에 화면 단위 파일로 나눴다 — `js/app.js`의 `import ... from "./ui.js"`가 안 바뀌게
`ui.js`는 각 화면 파일의 렌더 함수를 다시 내보내는 배럴로만 남겨둔다. 새 화면을 추가할 때도
같은 패턴(화면별 파일 + `ui.js`에 재export 한 줄 추가)을 따른다.

## 4. 데이터 저장 규칙

- 저장소: `localStorage` 사용 (데이터 양이 많아져 성능 문제가 생기면 IndexedDB로 전환 검토)
- 모든 변경은 즉시 자동 저장한다. 별도의 "저장" 버튼을 두지 않는다.
- 저장하는 최상위 객체에 스키마 버전 필드(`schemaVersion`)를 포함해 향후 구조 변경 시
  마이그레이션할 수 있게 한다.
- **데이터를 읽을 때 `schemaVersion`이 다르다고 곧바로 초기화하지 않는다.** `storage.js`의
  `normalize()`처럼, 배열(`goals`, `entries`)이 존재하는 한 누락된 필드는 기본값으로
  채워서 기존 데이터를 최대한 살린다. 완전히 읽을 수 없는 경우에만 빈 데이터로 초기화한다.
  앱을 배포(코드 업데이트)해도 브라우저의 `localStorage` 자체는 그대로 남아있으므로,
  이 규칙을 지키면 배포 때문에 사용자의 기존 기록이 사라지는 일이 없다.
- JSON 내보내기/가져오기는 저장 포맷과 1:1로 매핑되는 전체 데이터 덤프여야 한다
  (선택적 부분 백업 아님).
- iOS Safari가 로컬 저장소를 지울 수 있다는 전제하에, 앱 내에 "마지막 백업 이후 경과일"
  같은 안내를 표시해 사용자가 주기적으로 백업하도록 유도하는 것을 고려한다 (v1 필수 아님,
  가능하면 포함).

## 5. 코드 스타일

- 주석은 최소화한다. 코드만 읽어서는 알 수 없는 "왜"가 있을 때만 짧게 남긴다.
- 도메인 용어는 코드 상에서 영어로 통일한다: 과목 → `subject`, 교재 → `material`,
  회독 → `round`, 목표 → `goal`. (UI 표시 텍스트는 한국어 그대로.)
- 전역 변수를 최소화하고, 각 `js` 파일은 명시적으로 필요한 함수/데이터만 주고받는다
  (모듈 스크립트 `type="module"` + `import`/`export` 사용).
- 함수는 하나의 책임만 갖도록 작게 유지한다. 특히 `stats.js`의 계산 함수는 입력을 받아
  값을 반환하는 순수 함수로 작성해 테스트/검증이 쉬워야 한다.

## 6. UI / 반응형

- 모바일 퍼스트: 아이폰 화면 크기(390~430px 너비 대) 기준으로 우선 설계한다.
- 데스크톱 대응은 필수가 아니지만, 최소한 레이아웃이 깨지지 않는 정도는 유지한다.
- 터치 타깃(버튼 등)은 최소 44x44px 확보.
- 비주얼 톤: 토스(Toss) 앱 느낌의 모던한 핀테크 스타일을 기준으로 한다.
  - 카드에 얇은 테두리(border) 대신 은은한 그림자(box-shadow)로 구분을 준다.
  - 모서리는 큼직하게 둥글린다 (카드 `--radius: 20px`, 버튼/입력창 `--radius-sm: 14px`).
  - 큰 수치(달성률, 누적 시간 등)는 굵고(font-weight 800) 살짝 좁은 자간으로 강조한다.
  - 버튼/탭 클릭 시 `:active`에서 살짝 축소(`scale(0.97)`)되는 피드백을 준다.
  - 하단 탭바는 하드 보더 대신 위쪽 그림자로 구분하고, 활성 탭은 배경 없이
    색상+굵은 글씨만으로 표시한다.
  - 색상 팔레트 자체(파란 accent, 카테고리 8색, 상태색)는 dataviz 스킬로 검증된 값을
    그대로 유지하고, 모양·그림자·타이포로만 톤을 바꾼다.

## 7. 배포

- GitHub Pages에 배포한다. 정적 파일만으로 구성되므로 특정 서버 런타임(Node 서버 등)에
  의존하는 코드를 작성하지 않는다.
- 리포지토리 루트(또는 `docs/` 폴더)에서 바로 서빙 가능한 구조를 유지한다.
- 정적 파일을 바꿔 배포할 때마다 `service-worker.js`의 캐시 버전(`cta-static-vN`)을 올리고,
  `CHANGELOG.md` 맨 위에 같은 버전 번호로 사용자 관점의 변경 내용을 추가한다. 개발 과정과 이유는
  `WORKLOG.md`에 남긴다(두 파일의 역할이 다르다: CHANGELOG는 "무엇이 바뀌었나", WORKLOG는 "왜·어떻게").

## 8. 하지 말아야 할 것

- 서버/백엔드 코드 작성 (앱은 완전히 클라이언트 사이드). 인증키가 필요한 외부 API는 앱에서 직접
  호출하지 않고, GitHub Actions가 받아 정적 파일(`data/`)로 저장하게 한다. API 키를 코드/저장소에
  넣지 않는다(GitHub Secrets 사용).
- 로그인/인증, 다중 사용자 대비 코드
- 사용하지 않는 기능을 위한 추상화나 설정 옵션 미리 만들기
